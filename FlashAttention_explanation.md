# 深度解析：FlashAttention 算法原理、数学推导与数值实例

本篇文档对 FlashAttention 算法进行深入剖析。我们将结合板书 `FlashAttention.png` 中的内容，详细对比标准注意力机制（Standard Attention）与 FlashAttention 的差异，推导在线 Softmax（Online Softmax）的核心公式，并通过一个具体的数值计算实例来演示算法的等价性与硬件优化原理。

---

## 一、 `FlashAttention.png` 板书结构解读

板书清晰地展示了标准注意力机制的瓶颈、GPU 硬件内存结构以及 FlashAttention 的破局之道。我们可以将其划分为以下四个部分：

### 1. GPU 内存层次结构（Memory Hierarchy）
板书右上角的金字塔模型展示了 GPU 的三级存储体系：
* **SRAM（静态随机存取内存）**：
  * **容量**：极小（约 20 MB）。
  * **带宽**：极高（约 19 TB/s）。
  * **位置**：位于 SM（流式多处理器）内部，作为片上高速缓存。
* **HBM（高带宽显存）**：
  * **容量**：中等（约 40 GB / 80 GB）。
  * **带宽**：中等（约 1.5 TB/s）。
  * **位置**：GPU 的主要全局显存。
* **DRAM（系统内存 / 主存）**：
  * **容量**：极大（大于 1 TB）。
  * **带宽**：极低（约 12.8 GB/s，通过 PCIe 连接）。

**核心痛点**：GPU 的计算速度（FLOPS）极快，但 HBM 的读写带宽（Bandwidth）相对较慢。标准注意力机制在计算过程中产生了大量的临时大矩阵（如注意力得分矩阵），这些矩阵必须反复在 HBM 和 SRAM 之间搬运，导致计算单元（ALUs）大部分时间都在等待数据传输，形成了严重的**访存瓶颈（Memory-Bound / IO-Bound）**。

---

### 2. 标准注意力机制（Standard Attention）的 HBM 读写瓶颈
板书左下角列出了标准注意力（Standard Attention）的 5 个步骤，每一步都需要将数据从 HBM 加载到 SRAM 计算，然后再写回 HBM：

**标准计算公式**：

$$
\text{Attn}(Q, K, V) = \text{softmax}(Q K^T) V
$$

假设序列长度为 $T$ ，特征维度为 $d$ ：
1. **加载 $Q, K^T$** 到 SRAM $\to$ 矩阵乘法 $\to$ 得到得分矩阵 $S = Q K^T$（大小为 $T \times T$）$\to$ **写入 HBM**。
2. **加载 $S$** 到 SRAM $\to$ 计算每一行的最大值 $m$（大小为 $T$）$\to$ **写入 HBM**。
3. **加载 $S$** 到 SRAM $\to$ 计算每行指数和 $S_{\text{sum}}$（大小为 $T$）$\to$ **写入 HBM**。
4. **加载 $S$** 到 SRAM $\to$ 进行 Softmax 归一化，得到注意力权重矩阵 $A = \text{softmax}(S)$（大小为 $T \times T$）$\to$ **写入 HBM**。
5. **加载 $A, V$** 到 SRAM $\to$ 矩阵乘法 $\to$ 得到输出矩阵 $O = A V$（大小为 $T \times d$）$\to$ **写入 HBM**。

**结论**：在整个计算过程中，中间的 $T \times T$ 大小矩阵 $S$ 和 $A$ 被反复读写 HBM 达多次。当序列长度 $T$ 增长时（例如从 2K 增长到 32K 或 128K），$O(T^2)$ 的读写开销和显存占用会急剧暴涨，导致显存溢出（OOM）和严重的运行速度拖累。

---

### 3. 数值稳定性与 Safe Softmax
为了防止指数计算时发生数值溢出（Overflow），板书中部指出了 **Safe Softmax** 的计算方法：

$$
\text{Safe Softmax}(x_i) = \frac{e^{x_i - m}}{\sum_{j} e^{x_j - m}}
$$

其中 $m$ 是输入向量中的最大值：

$$
m = \max_{j} x_j
$$

通过减去最大值 $m$ ，保证了所有的指数项都在 $(0, 1]$ 之间，避免了 $\exp(x_i)$ 直接溢出为无穷大（$\infty$）。

---

### 4. FlashAttention 的分块（Tiling）思想
板书右下角展示了 FlashAttention 的核心思想——**分块（Tiling）**与**在线 Softmax 更新（Online Softmax Update）**：
* **核心思想**：不一次性计算完整的 $T \times T$ 注意力矩阵，而是将输入矩阵 $Q, K, V$ 划分为若干个小块（Tiles）。
* **块大小设计**：选择合适的块大小 $B_r$ 和 $B_c$ ，使得每次只加载小块 $Q_i \in \mathbb{R}^{B_r \times d}$ , $K_j \in \mathbb{R}^{B_c \times d}$ , $V_j \in \mathbb{R}^{B_c \times d}$ 到 SRAM 中。
* **片上融合**：在 SRAM 中完成局部点积、局部归一化以及与 $V_j$ 的局部累加。通过在线重缩放机制，将局部的注意力结果逐步融合，从而**在整个前向传播过程中，完全不需要向 HBM 写入 $O(T^2)$ 大小的中间矩阵**。

---

## 二、 在线 Softmax（Online Softmax）数学推导

在线 Softmax 是实现 FlashAttention 融合计算的关键数学工具。下面我们来推导当新的分块到来时，如何动态地更新最大值、指数和以及输出矩阵。

### 1. 变量定义
假设一个查询向量 $q$ 与两组键值对进行计算。第一组对应的原始注意力得分为 $S^{(1)} \in \mathbb{R}^{B_1}$ ，第二组对应的原始注意力得分为 $S^{(2)} \in \mathbb{R}^{B_2}$ 。

* 对于第一组（已计算部分）：
  * 局部最大值：
  
  $$
  m_1 = \max(S^{(1)})
  $$
  
  * 局部指数和：
  
  $$
  L_1 = \sum_{k} e^{S^{(1)}_k - m_1}
  $$
  
  * 局部归一化输出（这里简化为一维标量输出的权重）：
  
  $$
  O_1 = \frac{\sum_{k} e^{S^{(1)}_k - m_1} V_k^{(1)}}{L_1}
  $$

* 对于第二组（新到来部分）：
  * 局部最大值：
  
  $$
  m_2 = \max(S^{(2)})
  $$
  
  * 局部指数和：
  
  $$
  L_2 = \sum_{k} e^{S^{(2)}_k - m_2}
  $$

---

### 2. 全局状态的递推更新
我们希望在不保留 $S^{(1)}$ 的情况下，仅用 $m_1, L_1, O_1$ 和新到来的 $S^{(2)}, L_2, V^{(2)}$ 计算合并后的全局最大值 $m_{\text{new}}$ 、全局指数和 $L_{\text{new}}$ 以及全局归一化输出 $O_{\text{new}}$ 。

#### 步骤 1：更新最大值
合并后的全局最大值为：

$$
m_{\text{new}} = \max(m_1, m_2)
$$

#### 步骤 2：更新指数和
由于最大值从 $m_1$ 和 $m_2$ 变为了 $m_{\text{new}}$ ，我们需要对之前的指数和进行基准对齐：

$$
L_{\text{new}} = L_1 \cdot e^{m_1 - m_{\text{new}}} + L_2 \cdot e^{m_2 - m_{\text{new}}}
$$

这是因为：

$$
L_1 \cdot e^{m_1 - m_{\text{new}}} = \sum_{k} e^{S^{(1)}_k - m_1} \cdot e^{m_1 - m_{\text{new}}} = \sum_{k} e^{S^{(1)}_k - m_{\text{new}}}
$$

#### 步骤 3：更新输出矩阵
原输出的未归一化分子为：

$$
P_1 = L_1 \cdot O_1 = \sum_{k} e^{S^{(1)}_k - m_1} V_k^{(1)}
$$

为了将分子对齐到新的全局最大值 $m_{\text{new}}$ 下，我们需要给旧分子乘上重缩放因子 $e^{m_1 - m_{\text{new}}}$ ：

$$
P_1 \cdot e^{m_1 - m_{\text{new}}} = \sum_{k} e^{S^{(1)}_k - m_{\text{new}}} V_k^{(1)}
$$

新到来的部分的未归一化分子（对齐到 $m_{\text{new}}$）为：

$$
P_2 \cdot e^{m_2 - m_{\text{new}}} = \left( \sum_{k} e^{S^{(2)}_k - m_2} V_k^{(2)} \right) \cdot e^{m_2 - m_{\text{new}}}
$$

将它们相加并除以新的全局指数和 $L_{\text{new}}$ ，得到更新后的输出 $O_{\text{new}}$ ：

$$
O_{\text{new}} = \frac{L_1 \cdot e^{m_1 - m_{\text{new}}}}{L_{\text{new}}} \cdot O_1 + \frac{e^{m_2 - m_{\text{new}}}}{L_{\text{new}}} \cdot \sum_{k} e^{S^{(2)}_k - m_2} V_k^{(2)}
$$

这正是板书右下角对应的三个核心公式：

$$
m_t = \max(m_{t-1}, m_{\text{local}})
$$

$$
S_t = S_{t-1} \cdot e^{m_{t-1} - m_t} + \sum e^{\text{Result}_{\text{local}} - m_t}
$$

$$
O_t = \left[ \frac{S_{t-1} \cdot e^{m_{t-1} - m_t}}{S_t} \right] O_{t-1} + \left[ \frac{e^{\text{Result}_{\text{local}} - m_t}}{S_t} \right] V_j
$$

*(注：此处板书中的 $S_t$ 即为我们推导中的指数和 $L_t$)*

---

## 三、 具体数值计算案例演示

为了最直观地展示标准注意力与 FlashAttention 的等价性，我们设置一个具体的简单数值例子。

### 1. 基础数据设定
* **查询向量 (Query)**：仅包含 1 个 Token ($T_q = 1$)，其与 Key 的未归一化原始得分（logits）为：
  
  $$
  S = [2.0, 4.0, 1.0, 3.0]
  $$
  
* **值向量 (Value)**：序列长度为 4 ($T_k = 4$)，特征维度为 2 ($d = 2$)：
  
  $$
  V_1 = [10.0, 20.0]^T, \quad V_2 = [30.0, 40.0]^T, \quad V_3 = [50.0, 60.0]^T, \quad V_4 = [70.0, 80.0]^T
  $$

---

### 2. 标准注意力计算步骤（Standard Attention）
按照传统的 Safe Softmax 计算流程：

**第一步：求全局最大值**

$$
m = \max(2.0, 4.0, 1.0, 3.0) = 4.0
$$

**第二步：计算指数值（已减去最大值 $m$）**
* $e_1 = e^{2.0 - 4.0} = e^{-2.0} \approx 0.13534$
* $e_2 = e^{4.0 - 4.0} = e^{0.0} = 1.00000$
* $e_3 = e^{1.0 - 4.0} = e^{-3.0} \approx 0.04979$
* $e_4 = e^{3.0 - 4.0} = e^{-1.0} \approx 0.36788$

**第三步：求指数之和**

$$
S_{\text{sum}} = 0.13534 + 1.00000 + 0.04979 + 0.36788 = 1.55301
$$

**第四步：计算注意力权重 $\alpha$**
* $\alpha_1 = \frac{0.13534}{1.55301} \approx 0.08715$
* $\alpha_2 = \frac{1.00000}{1.55301} \approx 0.64391$
* $\alpha_3 = \frac{0.04979}{1.55301} \approx 0.03206$
* $\alpha_4 = \frac{0.36788}{1.55301} \approx 0.23688$

**第五步：加权求和计算最终输出 $O$**

$$
O = \alpha_1 V_1 + \alpha_2 V_2 + \alpha_3 V_3 + \alpha_4 V_4
$$

分别计算 $O$ 的两个维度：
* 维度 1：
  
  $$
  O_x = 0.08715 \cdot 10.0 + 0.64391 \cdot 30.0 + 0.03206 \cdot 50.0 + 0.23688 \cdot 70.0
  $$
  
  $$
  O_x = 0.8715 + 19.3173 + 1.6030 + 16.5816 = 38.3734
  $$
  
* 维度 2：
  
  $$
  O_y = 0.08715 \cdot 20.0 + 0.64391 \cdot 40.0 + 0.03206 \cdot 60.0 + 0.23688 \cdot 80.0
  $$
  
  $$
  O_y = 1.7430 + 25.7564 + 1.9236 + 18.9504 = 48.3734
  $$

得到最终的注意力输出：

$$
O \approx [38.3734, 48.3734]^T
$$

---

### 3. FlashAttention 分块计算步骤（Tiling, $B_c = 2$）
现在我们模拟 FlashAttention。我们将输入分为 2 个块（每个块大小为 2）：
* **Block 1**：对应 $S^{(1)} = [2.0, 4.0]$，值向量为 $V_1, V_2$。
* **Block 2**：对应 $S^{(2)} = [1.0, 3.0]$，值向量为 $V_3, V_4$。

我们在 SRAM 中逐步更新运行状态。初始状态设为：$m_0 = -\infty$ , $S_0 = 0$ , $O_0 = [0, 0]^T$。

#### 步骤一：处理第一个分块 Block 1
1. **计算局部最大值**：
   
   $$
   m_1 = \max(2.0, 4.0) = 4.0
   $$
   
2. **计算局部指数和**：
   
   $$
   S_1 = e^{2.0 - 4.0} + e^{4.0 - 4.0} = e^{-2.0} + e^0 = 0.13534 + 1.00000 = 1.13534
   $$
   
3. **计算局部的归一化输出 $O_1$**：
   
   $$
   O_1 = \frac{e^{2.0 - 4.0} V_1 + e^{4.0 - 4.0} V_2}{S_1} = \frac{0.13534 \cdot [10, 20]^T + 1.0 \cdot [30, 40]^T}{1.13534}
   $$
   
   $$
   O_1 = \frac{[1.3534 + 30.0, 2.7068 + 40.0]^T}{1.13534} = \frac{[31.3534, 42.7068]^T}{1.13534} \approx [27.6159, 37.6159]^T
   $$

在处理完第一个块后，保留在 SRAM 寄存器中的状态为：
* $m_1 = 4.0$
* $S_1 = 1.13534$
* $O_1 \approx [27.6159, 37.6159]^T$

#### 步骤二：处理第二个分块 Block 2
新到来的数据为 $S^{(2)} = [1.0, 3.0]$ ， $V_3, V_4$。
1. **计算 Block 2 局部最大值**：
   
   $$
   m_{\text{local}} = \max(1.0, 3.0) = 3.0
   $$
   
2. **更新全局最大值**：
   
   $$
   m_2 = \max(m_1, m_{\text{local}}) = \max(4.0, 3.0) = 4.0
   $$
   
3. **更新全局指数和 $S_2$**：
   
   $$
   S_2 = S_1 \cdot e^{m_1 - m_2} + \sum e^{S^{(2)} - m_2}
   $$
   
   代入具体数值：
   
   $$
   S_2 = 1.13534 \cdot e^{4.0 - 4.0} + (e^{1.0 - 4.0} + e^{3.0 - 4.0})
   $$
   
   $$
   S_2 = 1.13534 \cdot 1.0 + (e^{-3.0} + e^{-1.0})
   $$
   
   $$
   S_2 = 1.13534 + (0.04979 + 0.36788) = 1.13534 + 0.41767 = 1.55301
   $$
   
   *(可以看到，更新后的指数和 $S_2$ 完美等于标准计算的 $S_{\text{sum}}$)*

4. **更新输出向量 $O_2$**：
   根据递推更新公式：
   
   $$
   O_2 = \left[ \frac{S_1 \cdot e^{m_1 - m_2}}{S_2} \right] O_1 + \left[ \frac{e^{S^{(2)} - m_2}}{S_2} \right] V^{(2)}
   $$
   
   我们将两部分分别计算：
   * **第一项（旧分块贡献的对齐重缩放）**：
     * 重缩放系数：
     
     $$
     \beta = \frac{S_1 \cdot e^{4.0 - 4.0}}{S_2} = \frac{1.13534 \cdot 1.0}{1.55301} \approx 0.73106
     $$
     
     * 乘上旧输出 $O_1$：
     
     $$
     \text{Term}_1 = 0.73106 \cdot [27.6159, 37.6159]^T \approx [20.1889, 27.5000]^T
     $$
     
   * **第二项（新分块贡献）**：
     
     $$
     \text{Term}_2 = \frac{e^{1.0 - 4.0} V_3 + e^{3.0 - 4.0} V_4}{S_2} = \frac{0.04979 \cdot [50.0, 60.0]^T + 0.36788 \cdot [70.0, 80.0]^T}{1.55301}
     $$
     
     $$
     \text{Term}_2 = \frac{[2.4895 + 25.7516,  2.9874 + 29.4304]^T}{1.55301} = \frac{[28.2411, 32.4178]^T}{1.55301} \approx [18.1848, 20.8742]^T
     $$
     
   * **将两项相加合并**：
     
     $$
     O_2 = \text{Term}_1 + \text{Term}_2 \approx [20.1889 + 18.1848,  27.5000 + 20.8742]^T = [38.3737, 48.3742]^T
     $$

### 4. 案例小结
对比两种方法的计算结果：
* **标准注意力机制结果**：$[38.3734, 48.3734]^T$
* **FlashAttention 结果**：$[38.3737, 48.3742]^T$

二者数值完全一致（微小的误差仅仅来自于中间步骤的有限有效数字舍入）。这在数学上证明了：**FlashAttention 通过分块分步计算得到的最终结果，与全局计算出的 Softmax 注意力结果完全等价，没有任何精度损失！**

---

## 四、 硬件性能与算法效率分析

### 1. 显存与访存优化对比
| 维度 | 标准注意力机制 (Standard Attention) | FlashAttention |
| :--- | :--- | :--- |
| **HBM 显存占用（额外）** | $O(T^2)$（需存储完整的注意力矩阵 $S$ 和 $A$） | $O(1)$ 或 $O(T \cdot d)$（完全没有中间 $T^2$ 矩阵） |
| **HBM 访存次数（IO 复杂度）** | $O(T^2 \cdot d)$ | $O(\frac{T^2 \cdot d^2}{M})$，其中 $M$ 为 SRAM 大小 |
| **计算复杂度 (FLOPs)** | $O(T^2 \cdot d)$ | $O(T^2 \cdot d)$ |
| **计算瓶颈类型** | Memory-bound (带宽限制) | Compute-bound (算力限制) |

FlashAttention 虽然没有减少矩阵乘法的计算次数（FLOPs 相同），但它成功将注意力计算从**访存瓶颈（Memory-Bound）**转变成了**算力瓶颈（Compute-Bound）**，在 GPU（如 A100）上实现了 2~4 倍的实际运行速度提升。

### 2. 反向传播中的重计算（Recomputation）策略
为了在反向传播（Backward Pass）中也实现 $O(T^2)$ 零显存占用的特性，FlashAttention 采用了**重计算**的策略：
* **传统反向传播**：需要依赖前向传播保存的注意力权重矩阵 $A \in \mathbb{R}^{T \times T}$ 来计算关于 $Q, K, V$ 的梯度，这会导致前向阶段的显存暴涨。
* **FlashAttention 反向传播**：前向阶段**不保存** $A$ 矩阵，只保存每行的最大值 $m$ 和指数和 $S_{\text{sum}}$。在反向阶段，当需要 $A$ 矩阵计算梯度时，利用 SRAM 中的分块，从 HBM 加载 $Q_i, K_j$ **重新计算**局部的 $A_{ij}$。
* **空间换时间**：虽然这增加了一次前向计算的 FLOPs 开销，但由于避免了极其缓慢的 HBM $A$ 矩阵读取过程，实际的总运行速度反而比保存 $A$ 矩阵的方法要快得多。
