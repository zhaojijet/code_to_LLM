# 全局工程协作规则

以下规则适用于所有项目。项目内更具体的 `AGENTS.md`、用户当前任务要求以及系统约束具有更高优先级。

## 1. 写之前先读（Read Before You Write）

- 修改代码前，先完整阅读将要修改的文件、直接调用方、相关测试和配置。
- 查找并遵循项目已有的结构、命名、错误处理和依赖使用方式。
- 不凭训练记忆假设项目使用某个框架、库、接口或版本；先从仓库和可用文档中确认。
- 找不到现有模式时，说明观察结果。只有当不同选择会实质影响结果且无法安全推断时，才向用户确认。

## 2. 编码前先想清楚（Think Before You Code）

- 在动手前明确目标、边界、成功标准以及关键假设。
- 对存在实际取舍的方案，简要说明取舍并给出推荐；不要罗列没有决策价值的选项。
- 简单、低风险、可逆的任务可直接执行；复杂的多步骤任务或涉及架构方向的任务先给出简短计划。
- 如果需求存在会显著改变实现方向的歧义，停止猜测并请求澄清。

## 3. 保持简单（Simplicity）

- 编写解决当前问题所需的最少代码，不为假想的未来需求提前设计。
- 优先直接、清晰、局部的实现，避免不必要的抽象、配置层、兼容层和框架。
- 不为理论上不可能或不在范围内的情况增加复杂错误处理。
- 如果某个抽象的唯一理由是“以后可能会用”，暂不引入。

## 4. 做精准改动（Surgical Changes）

- 只修改完成任务所必需的文件和代码行。
- 保持现有风格，不顺手重构、重命名、重排 import 或格式化无关代码。
- 仅清理由本次改动直接造成的未使用代码；已有技术债不在任务范围内时只需指出。
- 交付前检查 diff，确保每一处变化都能直接对应用户需求或必要验证。

## 5. 用验证证明结果（Verification）

- 不把“看起来正确”当作“已经正确”；使用测试、类型检查、构建、lint 或可复现的运行结果验证。
- 修复 Bug 时，在可行且与风险相称的情况下，先建立能失败的测试或最小复现，再实施修复并确认其通过。
- 测试可观察行为和真实风险，不为实现细节或无意义属性编写脆弱测试。
- 生成文档、表格、图片、图表或其他文件后，确认产物实际存在，并通过回读、渲染或打开检查其内容与布局。
- 运行与改动范围相称的验证；如无法运行、测试原本失败或验证受限，明确说明具体原因与剩余风险。

## 6. 以可验证目标驱动执行（Goal-Driven Execution）

- 把模糊请求转化为可检查的完成条件，再据此实施和验证。
- 复杂的多步骤任务保持一个简洁计划，并在发现关键假设错误、证据冲突或方向偏离时及时调整。
- 不以“代码已写完”作为完成标准；完成意味着目标行为已实现并获得适当验证。
- 在仍有安全、相关且可执行的下一步时继续推进，不把可自行解决的问题交还给用户。

## 7. 调试时调查而非猜测（Debugging）

- 阅读完整错误信息、堆栈、日志和相关代码路径，先稳定复现问题。
- 基于证据形成假设，一次改变一个关键变量，并在每步后检查结果。
- 修复根因，不用宽泛异常捕获、静默失败、随意重试或无依据的空值保护掩盖症状。
- 无法确定根因时，清楚列出已验证事实、尝试过的方法和仍不确定的部分。

## 8. 谨慎管理依赖（Dependencies）

- 新增依赖前，先检查标准库、现有依赖和项目内工具能否满足需求。
- 新依赖必须带来明确价值，并考虑维护状态、安全、体积、许可证和运行环境影响。
- 不悄悄加入生产依赖；说明加入原因，并按项目要求更新锁文件和相关文档。
- 不为了少量简单功能引入重量级依赖。

## 9. 清楚沟通（Communication）

- 先说明结果，再补充关键实现、验证情况和用户需要知道的风险。
- 解释做了什么以及为什么；避免只贴代码、命令输出或大段过程记录。
- 对不确定性使用具体表述，例如指出未经验证的假设和建议的验证方式。
- 不声称运行过未运行的测试，不把“应该可行”表述为“已经确认”。

## 10. 识别常见失败模式（Common Failure Modes）

- **Kitchen Sink：** 借任务之机大范围重构或清理无关代码。
- **Wrong Abstraction：** 在模式尚未稳定前过早抽象，或强行复用并不相同的逻辑。
- **Optimistic Path：** 只处理成功路径，忽略输入错误、外部失败和重要边界条件。
- **Runaway Refactor：** 一个局部修复不断扩散到更多文件和系统层级。
- 发现上述模式时，暂停扩张，回到原始目标，缩小改动，并在确需扩大范围时先说明原因与影响。
